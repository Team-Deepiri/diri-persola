name: "CodeQL"

on:
  push:
    branches: [ "main", "sandeep-dev" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 6 * * 1'  # Weekly on Mondays

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python', 'javascript' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      if: matrix.language == 'python'
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Poetry
      if: matrix.language == 'python'
      run: |
        curl -sSL https://install.python-poetry.org | python3 -

    - name: Update Poetry lock
      if: matrix.language == 'python'
      run: |
        poetry lock

    - name: Install Python dependencies
      if: matrix.language == 'python'
      run: |
        poetry install

    - name: Setup Node.js
      if: matrix.language == 'javascript'
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: ui/package-lock.json

    - name: Install UI dependencies
      if: matrix.language == 'javascript'
      run: |
        cd ui
        npm ci

    - name: Build UI
      if: matrix.language == 'javascript'
      run: |
        cd ui
        npm run build

    # Skip CodeQL analysis to avoid SARIF conflict with default setup
    # The build steps above validate code compiles correctly
